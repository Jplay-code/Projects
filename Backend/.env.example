# Base URL (padrão já apontado no app)
OPEN_METEO_BASE=https://api.open-meteo.com/v1

# Timeout para requisições (em segundos)
REQUEST_TIMEOUT=5

# Cache TTL (segundos)
CACHE_TTL=60
CACHE_MAXSIZE=1024

# Porta do container
PORT=8080

# Logging
LOG_LEVEL=INFO
